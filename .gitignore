# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
.pytest_cache/
.coverage
htmlcov/
.tox/
.venv/
venv/
ENV/
env/

# Project
*.db
*.db-journal
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
data/storage/*.db
data/storage/*.db-journal
data/storage/*.db-shm
data/storage/*.db-wal
data/cache/
data/exports/

# Logs
logs/
*.log
*.log.*

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*.local
*.env

# Secrets
secrets/
credentials/
*.pem
*.key
*.crt

# API keys / tokens
config.json
api_keys.json

# Backtest results (keep structure, ignore large outputs)
backtest/results/*.png
backtest/results/*.html

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.bak
*.swp
*~

# Distribution
build/
dist/
*.egg

# Hermes agent local cache (auto-generated, no need to track)
.hermes-cache/
.hermes/

# Journal export (auto-regenerated locally by cron, see export_journal.py)
# Committed initial snapshot for Vercel static deploy demo.
# Live updates require webhook or Vercel KV (out of scope for static site).
#journal.json

# Backtest 1Y output (auto-regenerated locally by cron, see backtest/run_yearly.py)
# Committed initial snapshot for Vercel static deploy demo so the Backtest 1Y
# section on https://rx-0-unicorn.vercel.app/ has data on first load.
# Live updates would require webhook or Vercel KV (out of scope for static site).
#backtest.json
